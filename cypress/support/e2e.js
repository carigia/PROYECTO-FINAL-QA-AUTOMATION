// Este archivo se carga automáticamente antes de ejecutar los tests de Cypress.
// Centraliza la carga de soporte común para que todos los specs compartan la misma configuración.
// Acá se registran los comandos personalizados disponibles para toda la suite.
import './formsCommands'
import './commands'

